{
  "name": "damians-app",
  "description": "Heroku deployable app. udacity fsnd capstone project",
  "image": "heroku/python",
  "keywords": ["python" ],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test": "python app.py"
      }
    }
  }
}
